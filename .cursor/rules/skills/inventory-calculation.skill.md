# Skill: Inventory Calculation
Description: Используй при работе с инвентаризацией магазина (ввод "факт").

Формулы:
- Разница = было + доставлено - выставлено - факт
- Продано = max(0, было + доставлено - факт)
- Склад = max(0, факт - выставлено)