# PROJECT STATUS — StaffEfficiency (21 мая 2026)

## ✅ Уже реализовано
- Полная Smartshell интеграция (смены + каталог + **продажи товаров**)
- Ручная синхронизация + детальный статус (`salesCount`)
- Страница `/inventory` обновлена:
  - Добавлены колонки «Продано» и «Выручка ₽»
  - Данные из Smartshell отображаются в таблице
  - `SmartshellSyncStatus` показывает количество импортированных продаж

## Следующий шаг (приоритет)

**Пункт 3: Финальная интеграция новых полей в расчёты инвентаризации**

Нужно, чтобы:
- Формулы расчёта (`calculateInventoryLine`) учитывали уже импортированные `sold` и `revenueGoods`
- Автоматически подставлялись значения `sold` при загрузке смены
- В live-расчётах (разница, продано, склад) использовались реальные данные из Smartshell